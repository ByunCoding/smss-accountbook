# 가계부 앱 TODO

## 완료된 작업 (2026-02-02)
- [x] FAB(+) 버튼 숨김 - 모달 열릴 때 저장하기 버튼과 겹치는 문제 해결
- [x] 모달 z-index 10000으로 변경 (FAB 9999보다 높게)
- [x] "적자" → "초과", "예산 초과"로 부드러운 표현 변경

## 남은 작업

### 1. 모바일/PC 데이터 동기화 개선
**현재 상황:**
- 데이터 저장: Google Apps Script (서버) ✅
- 데이터 표시: localStorage 캐시 먼저 → 5분 후 서버에서 새로 가져옴

**문제:**
- 모바일에서 입력 → PC에서 바로 안 보임 (캐시 때문)
- PC에서 입력 → 모바일에서 바로 안 보임 (캐시 때문)

**해결 옵션:**
- [ ] A) 캐시 시간 줄이기 (5분 → 1분)
- [ ] B) "새로고침" 버튼 추가 (수동으로 서버 데이터 불러오기)
- [ ] C) 둘 다

**관련 코드 위치:**
- `index.html` 라인 2465: `const CACHE_EXPIRY = 5 * 60 * 1000; // 5분`
- `index.html` 라인 2512: `async function loadData()` - 데이터 로드 함수

### 2. 연속 저장 문제 (추가 테스트 필요)
- 저장 버튼 상태 초기화 로직 추가됨
- 실제 모바일에서 테스트 필요
