# 배포

index.html 변경사항을 커밋하고 GitHub Pages에 배포합니다.

1. `git status`로 변경된 파일 확인
2. `git diff --stat`으로 변경 요약 확인
3. 변경 내용을 분석하여 적절한 한글 커밋 메시지 생성 (feat/fix/refactor)
4. `git add index.html` (변경된 파일만)
5. `git commit` 후 `git push`
6. 배포 URL 안내: https://byuncoding.github.io/smss-accountbook/
